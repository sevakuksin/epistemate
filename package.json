{
  "name": "chess-variant-pilot",
  "private": true,
  "scripts": {
    "bootstrap": "node scripts/bootstrap.mjs",
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm run dev --prefix apps/server\" \"npm run dev --prefix apps/web\"",
    "dev:web": "npm run dev --prefix apps/web",
    "dev:server": "npm run dev --prefix apps/server",
    "build": "npm run build --prefix packages/shared && npm run build --prefix packages/engine && npm run build --prefix apps/server && npm run build --prefix apps/web",
    "test": "npm run test --prefix packages/engine"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
